@using Bsef19a041_Project.Models;
@model List<Products>


<div class="container mt-3">
  <h2></h2>  <table class="table table-dark table-hover">
    <thead>
      <tr>
          <th>ID</th>
        <th>Product Name</th>
        <th>Price</th>
        



      </tr>
    </thead>
    <tbody>
        @foreach (Bsef19a041_Project.Models.Products P in Model)
        {
            <tr>
                @*<td> @P.ImageName.Remove(P.ImageName.Length-4,4)</td>*@
                <td>@P.Id .</td>
                <td> @P.ImageName</td>
                <td>@P.Price</td>

               
            </tr>

        }
                <a class="btn btn-danger" asp-controller="Cart"  asp-action="PlaceOrder" type="button">Place Order</a>

    </tbody>
</table>
</div>