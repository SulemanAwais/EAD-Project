@using Bsef19a041_Project.Models;
@model Products
<script type="text/javascript">
    function Search() {

        //var data = {
        //    Email: $("#email").val(),
        //    Password: $("#password").val(),
        //}
        var data=$("#Search").serialize
        //        var data = $("#submitUser").keyup(function() {
//            search_table($this).val()
//        )};
//function search_table(value) {
//    $('#').each(function() {
//        var found = 'false';
//        #(this).each(function() {

//        })
//    })
}
        console.log(data);
        //alert(data);
        $.ajax({
            type: 'POST',
            url: '/Cart/Search',
            contentType: 'application/x-www-form-urlencoded; charset=utf-8',
            //data: JSON.stringify(data),
            data: data,
            success: function (result) {
                alert('Successfully received Data ');
                console.log(result);
            },
            error: function () {
                alert('Failed to receive the Data');
                console.log('Failed ');
            }
        })
    }
</script>
<style>
    body{
        background-color:black;
    }
    #logo{
        height:60%;
        width:60%
    }
    #main{
        display:flex;
        background-color:darkgray;
        margin-top:20px;
        margin-bottom:20px;
        text-align:center;
        width:80%;
        margin-left:10%;
        border-radius:15px;
    }
   
</style>



<div>
    <form id="Search" novalidate action="/Cart/Search" method="post">
        <input type="search"   asp-for="ImageName"/>
        <input type="submit" class="btn btn-dark"  onclick="Search()"value="Search"/>

    </form>

</div>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}